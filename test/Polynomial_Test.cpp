#include "Polynomial.h"
#include "gtest/gtest.h"

TEST(OKTEST, OKOK) {
    EXPECT_EQ(2, 2);
}

TEST(Polynomial_Test, create_polynomial) {
    EXPECT_NO_THROW(Polynomial());
}

TEST(Polynomial_Test, smth) {
    EXPECT_EQ(3, 3);
}
